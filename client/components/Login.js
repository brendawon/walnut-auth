import React from "react";

const Login = (props) => {
  return (
    <div>
      <form>
        <input name="username" />
        <input name="password" />
        <button>Sign In</button>
      </form>
    </div>
  );
};

export default Login;
